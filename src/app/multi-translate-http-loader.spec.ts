import { MultiTranslateHttpLoader } from './multi-translate-http-loader';

describe('MultiTranslateHttpLoader', () => {
  it('should create an instance', () => {
    expect(new MultiTranslateHttpLoader()).toBeTruthy();
  });
});
